.module_image_left= image_tag "audio_programs/round_image.png"

%ul#modules.module_selection_right
    %h1 Audio Programs
    - unless @audio_programs.blank?
        %p.select Select an Audio Program:
        - @audio_programs.each do |a|
            %li
                = image_tag "logo_pinwheel.png"
                = link_to a.title, my5_audio_program_path(a.id)
                %p= a.description.html_safe
    - else
        %p There are currently no Audio Programs.
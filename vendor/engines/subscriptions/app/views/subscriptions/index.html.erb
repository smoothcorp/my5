<% content_for :body_content_left do %>
  <ul id="subscriptions">
    <% @subscriptions.each do |subscription| %>
      <li>
        <%= link_to subscription, subscription_path(subscription) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
